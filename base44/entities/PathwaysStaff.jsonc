{
  "name": "PathwaysStaff",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Full name of the staff member"
    },
    "email": {
      "type": "string",
      "description": "Work email \u2014 used to match against logged-in users"
    },
    "role": {
      "type": "string",
      "enum": [
        "career_counsellor",
        "service_navigator",
        "admin",
        "manager"
      ],
      "default": "career_counsellor",
      "description": "Primary Pathways portal role"
    },
    "secondary_role": {
      "type": "string",
      "enum": [
        "career_counsellor",
        "service_navigator",
        "admin",
        "manager"
      ],
      "description": "Optional secondary Pathways portal role (blank if none). Lets a staff member appear in assignment dropdowns for this role in addition to their primary role."
    },
    "tertiary_role": {
      "type": "string",
      "enum": [
        "career_counsellor",
        "service_navigator",
        "admin",
        "manager"
      ],
      "description": "Optional tertiary Pathways portal role (blank if none). Lets a staff member appear in assignment dropdowns for this role in addition to their primary and secondary roles."
    },
    "is_active": {
      "type": "boolean",
      "default": true,
      "description": "Whether this staff member is currently active in the Pathways portal"
    }
  },
  "required": [
    "name",
    "email",
    "role"
  ]
}